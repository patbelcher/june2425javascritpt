//from edabit - very easy
//i started this but was waaay off base
const printArray = (arr) => {
    const result = [];
    for(let i = 1; i <= arr; i++){
        result.push(i);
    }
        return result;
}

//examples
console.log(printArray(1));
console.log(printArray(6));